# Resume PDFs go here

Drop these two files into `/public/` before shipping:

- `resume-ml.pdf` — the ML-focused resume variant
- `resume-security.pdf` — the security-focused resume variant

They're referenced from:
- `src/data/personal.ts` (the `resumes` object)
- `TerminalHero.tsx` (hero download buttons)
- `ContactPage` (contact-page download buttons)

Once the PDFs are in place, delete this file.
